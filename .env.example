# Forge Orchestrator Configuration
# Copy to .env and customize as needed

# =============================================================================
# LLM Provider API Keys (at least ONE required)
# =============================================================================
# You only need ONE of these. OpenRouter is recommended as it provides
# access to multiple models from different providers with a single key.

# OpenRouter (recommended) - https://openrouter.ai/keys
# Provides access to Claude, GPT-4, Gemini, Llama, and many more models
OPENROUTER_API_KEY=

# Direct provider keys (alternative to OpenRouter)
# Use these if you prefer direct API access or don't have OpenRouter

# OpenAI - https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic - https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Google AI Studio - https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# =============================================================================
# Server Settings
# =============================================================================

# Host and port for the orchestrator server
ORCHESTRATOR_HOST=0.0.0.0
ORCHESTRATOR_PORT=4041

# Armory gateway URL (MCP endpoint)
ORCHESTRATOR_ARMORY_URL=http://localhost:4042/mcp

# =============================================================================
# LLM Settings
# =============================================================================

# Default model - supports multiple formats:
#
# OpenRouter format (when using OPENROUTER_API_KEY):
#   anthropic/claude-sonnet-4
#   openai/gpt-4o
#   google/gemini-2.0-flash-exp
#
# Direct provider format (with explicit prefix):
#   openai:gpt-4o              (requires OPENAI_API_KEY)
#   anthropic:claude-sonnet-4-20250514  (requires ANTHROPIC_API_KEY)
#   google-gla:gemini-2.0-flash         (requires GEMINI_API_KEY)
#
# Auto-detect format (routes based on available API keys):
#   gpt-4o        -> uses OPENAI_API_KEY if set, else OpenRouter
#   claude-sonnet -> uses ANTHROPIC_API_KEY if set, else OpenRouter
#   gemini-flash  -> uses GEMINI_API_KEY if set, else OpenRouter
#
ORCHESTRATOR_DEFAULT_MODEL=anthropic/claude-sonnet-4

# Enable mock LLM for testing (no API calls)
ORCHESTRATOR_MOCK_LLM=false

# Stream thinking tokens if model supports it
ORCHESTRATOR_SHOW_THINKING=true

# =============================================================================
# SSE Settings
# =============================================================================

# Seconds between heartbeat ping events
ORCHESTRATOR_HEARTBEAT_INTERVAL=15

# Seconds before showing "tool still working" message
ORCHESTRATOR_TOOL_TIMEOUT_WARNING=30
